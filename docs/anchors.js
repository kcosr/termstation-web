(function () {
  // Define hidden anchors to be inserted above specific sections by index.
  // These anchors are used for navigation but are not visible.
  // Example: index 0 (first section) = overview, index 2 (third section) = features
  window.TERMSTATION_ANCHORS = [
    { index: 0, id: 'overview-anchor' },
    { index: 1, id: 'features-anchor' },
  ];
})();

